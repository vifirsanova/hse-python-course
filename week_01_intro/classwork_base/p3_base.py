"""
Презентация 3 — базовый уровень.
Задачи про алгоритмы: проверка дубликатов и оценка сложности.

Здесь даны подсказки без тестов (или с подробными объяснениями).
"""

# ============================================================
# Задача 1. Проверка дубликатов (медленное решение)
# ============================================================
# Условие:
#   Дан список целых чисел nums. Необходимо определить,
#   содержит ли список повторяющиеся элементы.
#   Вернуть True, если хотя бы одно значение встречается
#   больше одного раза, иначе False.
#
# Ограничения:
#   0 <= n <= 100_000
#   -10^9 <= nums[i] <= 10^9
#
# Решение коллеги:
#   def has_duplicates(nums):
#       for i in range(len(nums)):
#           for j in range(i + 1, len(nums)):
#               if nums[i] == nums[j]:
#                   return True
#       return False
#
# Разбор:
#   - Внешний цикл выполняется до n раз, внутренний — до n раз.
#   - T(n) = O(n^2), S(n) = O(1).
#   - Для 100_000 различных элементов — порядка n^2 сравнений.
#
# Задание:
#   1) Реализуйте has_duplicates_slow (как у коллеги).
#   2) Реализуйте has_duplicates_fast с использованием set.
#      Подсказка: проходите по списку, добавляйте в set;
#      если число уже в set — вернуть True.
#   3) Оцените сложность обоих решений.

def has_duplicates_slow(nums: list) -> bool:
    # TODO: реализовать
    pass


def has_duplicates_fast(nums: list) -> bool:
    # TODO: реализовать
    pass


# ============================================================
# Задача 2. Оценка сложности
# ============================================================
# Определите временную сложность каждого фрагмента.
# Ответ — строка: "O(1)", "O(log n)", "O(n)", "O(n log n)", "O(n^2)".
#
# Фрагмент A:
#   for x in items:
#       print(x)
#
# Фрагмент B:
#   for x in items:
#       for y in items:
#           print(x, y)
#
# Фрагмент C:
#   i = 0
#   while i < n:
#       i *= 2
#
# Фрагмент D:
#   for x in items:
#       pass
#   for x in items:
#       for y in items:
#           pass

def complexity_a() -> str:
    # TODO
    pass


def complexity_b() -> str:
    # TODO
    pass


def complexity_c() -> str:
    # TODO
    pass


def complexity_d() -> str:
    # TODO
    pass


# ============================================================
# Подсказки
# ============================================================
# A: один цикл по n элементам -> O(n)
# B: вложенные циклы -> n * n -> O(n^2)
# C: i удваивается -> O(log n)
# D: последовательные фрагменты: O(n) + O(n^2) -> O(n^2)

if __name__ == "__main__":
    print("p4_base: заполните TODO в функциях.")
    print("Задачи: has_duplicates (slow/fast), оценка сложности")
