## Первая программа

```python
print("Hello, World!")
```

- `print()` — встроенная функция вывода.
- Строка в кавычках — строковый литерал.

## Комментарии

```python
# однострочный
print("")  # тоже комментарий
```

Docstring:

```python
"""
Описание модуля.
"""
```

## Переменные

Именованная ссылка на объект в памяти.

```python
name = "John"
age = 25
pi = 3.14
is_student = True
```

Правила имён:
- буквы, цифры, `_`;
- не начинается с цифры;
- регистрозависимо;
- осмысленно: `user_name`, не `x1`;
- стиль: `snake_case`.

Зарезервировано: `if`, `for`, `while`, `class`, `def`, `return` и др.

## Присваивание

```python
x = 10
y = x
x = 20
print(y)  # 10
```

Множественное:

```python
a, b = 1, 2
a, b = b, a
```

Распаковка:

```python
x, y, z = [10, 20, 30]
first, *rest = [1, 2, 3, 4]
```

Составные операторы:

```python
x += 3
x -= 1
x *= 2
x /= 4
x //= 2
x %= 3
x **= 2
```

## Моржовый оператор `:=` (Python 3.8+)

```python
if (n := len(data)) > 10:
    print(n)

while (line := input()) != "stop":
    print(line)
```

## Арифметические операции

| Операция | Пример | Результат |
|----------|--------|-----------|
| `+` | `10 + 3` | `13` |
| `-` | `10 - 3` | `7` |
| `*` | `10 * 3` | `30` |
| `/` | `10 / 3` | `3.333...` |
| `//` | `10 // 3` | `3` |
| `%` | `10 % 3` | `1` |
| `**` | `10 ** 3` | `1000` |

Приоритет:
1. `()`
2. `**`
3. `*`, `/`, `//`, `%`
4. `+`, `-`
5. слева направо

Пример:

```python
2 + 3 * 4   # 14
(2 + 3) * 4 # 20
```
